(A) 2026-06-21 Reformat all 81 black-noncompliant files with pinned black 25.1.0 in a single style-only commit; run `pip install black==25.1.0 && black chiyoda tests scripts` then verify `pre-commit run --all-files` is clean +Style @config release:v3.1
(A) 2026-06-21 Add `CLAUDE.md` to `.gitignore` (line after `.env`) and `rm` the local symlink at repo root so the committed project CLAUDE.md is authoritative; verify `git ls-files | grep CLAUDE.md` returns the tracked file only +Style @config release:v3.1
(A) 2026-06-21 Commit the working-tree deletion of `TODO.md` *as the replacement step* of this file: new TODO.md content lives here; `git add TODO.md` after editing finishes the housekeeping +Repro @config release:v3.1
(A) 2026-06-21 Refresh `docs/implementation_audit.md` to document wildfire/flood/aftershock/shooter runtime paths (terrain damage, re-evacuation waves, ember sampling, range_m for shooter) at the same depth as the connector section +Docs @docs release:v3.1
(B) 2026-06-21 Register benchmark suite v2 in `chiyoda/studies/benchmark.py` covering `wildfire_wui` (+ existing `transit_shooter`); add `_spec_for_suite("v2")` branch, mirror `composite_v1` scoring, extend `submit_policy(suite=...)` test coverage +Benchmark @code release:v2
(B) 2026-06-21 Register benchmark suite v3 in `chiyoda/studies/benchmark.py` covering `flood_urban` and `quake_aftershock`; reuse BenchmarkScenario class, add v3 entry to `docs/benchmark/benchmark_spec_v1.json` family (create `benchmark_spec_v2.json` and `benchmark_spec_v3.json`) +Benchmark @code release:v3
(B) 2026-06-21 Add `tests/test_benchmark_v2_v3.py` asserting suite registration, schema validity, and seed reproducibility for the new scenarios (mirror `tests/test_benchmark.py`) +Benchmark @tests release:v3
(B) 2026-06-21 Pin a `requirements-lock.txt` generated via `pip-compile requirements.txt` (or `pip freeze` from `.venv` with comments stripped); reference it in `docs/reproducibility_kit.md` line 92 area +Repro @config release:v3.1
(B) 2026-06-21 Wire the CI `benchmark-smoke.yml` workflow to also run `pytest tests/test_benchmark_v2_v3.py` on the `benchmark-smoke` label +Benchmark @ci release:v3
(B) 2026-06-21 Update `docs/architecture_overview.md` line 77 scenario list and the Benchmark Layer section to include v2/v3 scenarios and suite knobs +Docs @docs release:v3
(B) 2026-06-21 Update `docs/paper_outline_info_warfare.md` benchmark section table to list v2/v3 scenarios as separate rows with stressor descriptions +Docs @docs release:v3
(C) 2026-06-21 Add Gym/PettingZoo-style adapter `chiyoda/acceleration/rl_env.py` exposing `reset()`/`step(action)` where `action` keys mirror `policy['interventions']`; observation is `(belief_entropy, exposure, density, hci)` per active intervention slot; reuse `chiyoda/core/simulation.py:run_step` rather than reimplementing +RL @code release:v4
(C) 2026-06-21 Add `tests/test_rl_env_smoke.py` asserting a random-policy 100-step rollout terminates, returns finite reward, and matches a direct `Simulation` run when actions are no-op +RL @tests release:v4
(C) 2026-06-21 Author `docs/rl_interface.md` describing observation/action/reward and a worked stable-baselines3 PPO example using `scenarios/benchmark/transit_cbrn.yaml`; cite EvacuAI (PMC10648289) and arxiv 2509.19972 as prior art +RL @docs release:v4
(C) 2026-06-21 Extend `chiyoda/information/warfare.py` HostileChannel with a `target_persona` field selecting recipients by cohort/mobility/age band; route through existing homophily code paths in `chiyoda/information/propagation.py`; cite arxiv 2511.04697 in module docstring +Hostile @code release:v4
(C) 2026-06-21 Add `tests/test_hostile_persona_targeting.py` checking that persona-targeted decoy-exit claims affect only the targeted cohort's belief vectors while leaving other cohorts within tolerance of baseline +Hostile @tests release:v4
(C) 2026-06-21 Add `scripts/export_to_pedpy.py` that reads `out/<study>/trajectories.csv` and writes a PedPy-compatible SQLite via `pedpy.io.trajectory_to_sqlite` (or pandas equivalent); add `tests/test_pedpy_roundtrip.py` gated on `pedpy` import availability +Interop @code release:v3.1
(C) 2026-06-21 Add an LLM-as-judge validator hook in `chiyoda/information/llm.py`: optional second-pass provider call that scores `(safety, specificity, ground-truth-alignment)` and rejects below threshold; record judge verdict in `llm_calls` study export schema +LLM @code release:v4
(C) 2026-06-21 Add a counterfactual scoring slice to `chiyoda/studies/benchmark.py`: alongside `composite_v1`, compute `delta_vs_no_intervention` using `chiyoda/studies/causal.py` matched-pair machinery; expose as `composite_v1_causal` opt-in metric +Causal @code release:v4
(C) 2026-06-21 Write a worked FDS-to-imported-field example in `docs/hazard_field_import.md`: small synthetic FDS slice file checked into `tests/fixtures/` and an end-to-end CLI command using `ImportedHazardField.from_csv` (chiyoda/environment/hazards.py:468) +Hazards @docs release:v3.1
(C) 2026-06-21 Publish a community submissions doc `docs/benchmark/submitting.md`: PR template, required fields (policy YAML, reproducibility manifest, score), CI label `benchmark-submission`, append-only `docs/benchmark/leaderboard.jsonl` +Benchmark @docs release:v3.1
(D) 2026-06-21 Browser-side simulation loop spike: design doc only in `docs/3d_viewer_simulation.md`, no code; outline the JS port surface (cell graph, social-force step, belief updates) and what stays Python (LLM calls, study export). Decide stop/go before any implementation +Viewer @docs release:v4
(D) 2026-06-21 OpenStreetMap import CLI: extend `chiyoda/scenarios/standards.py` to accept an `--osm-bbox lat,lon,lat,lon` flag that fetches via Overpass and emits a strict scenario; reuse existing `kasumigaseki_osm_ci.yaml` provenance schema +Interop @code release:v4
(D) 2026-06-21 Red-team LLM sweep: add `scenarios/study_llm_red_team.yaml` where the LLM provider drives `hostile_channels` claim generation under budget guard; reuse `chiyoda/information/llm.py` provider abstraction and cache replay to keep CI deterministic +LLM @code release:v4
(B) 2026-06-21 Add `ruff` to `.pre-commit-config.yaml` + `pyproject.toml` `[tool.ruff]` block (line-length 88, target py312, select E/F/I/UP/B); run once and clean residual lints +DevEx @config release:v3.1
(B) 2026-06-21 Establish `mypy --strict` baseline: add `[tool.mypy]` to `pyproject.toml`, run on `chiyoda/`, write current error count to `docs/typing_baseline.md`, and gate new code on no-regression in CI +DevEx @config release:v3.1
(B) 2026-06-21 Replace the stub Julia path in `chiyoda/acceleration/backends.py:115` with a real `juliacall` social-force kernel; add `tests/test_julia_backend_parity.py` asserting numerical parity (<=1e-6) with the Python kernel on a 100-agent corridor +Perf @code release:v4
(B) 2026-06-21 Add structured logging: introduce a `chiyoda/_logging.py` helper that emits JSON when `CHIYODA_LOG_FORMAT=json` and human text otherwise; thread through `chiyoda/core/simulation.py` and `chiyoda/studies/runner.py` step/stage events +Usability @code release:v3.1
(C) 2026-06-21 Author `CONTRIBUTING.md` at repo root: branch model, commit-message style (conventional commits OK), local test policy (`make verify`), pre-commit setup, benchmark-submission flow link +DevEx @docs release:v3.1
(C) 2026-06-21 Add `CHANGELOG.md` (Keep-a-Changelog format) and backfill a `[3.0.0] - 2026-06-21` section summarizing benchmarks v1, hostile channels, LLM selective controls, route-choice calibration, 3D mobility, flood/aftershock hazards +DevEx @docs release:v3.1
(C) 2026-06-21 Add `.github/ISSUE_TEMPLATE/{bug.yml,feature.yml,benchmark_submission.yml}` and `.github/PULL_REQUEST_TEMPLATE.md`; the benchmark-submission template requires policy hash, suite, mean score, seeds, manifest hash +DevEx @config release:v3.1
(C) 2026-06-21 Add `codespell` pre-commit hook with `--ignore-words-list` for `nd,jupyter,iy` etc.; fix surfaced typos in docs and code comments +DevEx @config release:v3.1
(C) 2026-06-21 Add a `make profile` target running `python -m cProfile -o out/profile.prof scripts/profile_large_scenario.py` and documenting how to inspect with snakeviz in `docs/developer_environment.md` +DevEx @config release:v3.1
(C) 2026-06-21 numba `@njit` annotations on the hottest loops in `chiyoda/navigation/social_force.py` and `chiyoda/navigation/spatial_index.py`; gate behind `try: import numba` with a Python fallback; add a perf-regression smoke under `tests/test_benchmark.py` +Perf @code release:v4
(C) 2026-06-21 Parallelize seed-level study sweeps in `chiyoda/studies/runner.py` via `concurrent.futures.ProcessPoolExecutor`; expose `--jobs N` CLI flag in `chiyoda/cli.py` `sweep` command; keep deterministic ordering on output rows +Perf @code release:v4
(C) 2026-06-21 Add `scenarios/benchmark/transit_cbrn_10k.yaml` and a `tests/test_perf_10k.py` clock-budget assertion (<=60s on CI) so future regressions are caught; mark slow with `@pytest.mark.slow` and exclude from default `make test` +Perf @tests release:v4
(C) 2026-06-21 Vectorize belief updates in `chiyoda/information/field.py`: replace per-agent Python loops with numpy broadcasted ops on the belief matrix; verify with existing entropy/belief tests for bitwise-close equivalence +Perf @code release:v4
(C) 2026-06-21 Author a `Dockerfile` (multistage, `python:3.12-slim` base) at repo root and `docs/docker.md` showing `docker run chiyoda benchmark submit --suite v1`; include `requirements-lock.txt` from the (B) lock task as the install source +Usability @config release:v3.1
(C) 2026-06-21 Add optional IFC building-plan importer at `chiyoda/scenarios/ifc_import.py` using `ifcopenshell` (gated import) that lowers an IFC file to strict `layout.floors`; add `tests/test_ifc_import.py` with a tiny synthetic IFC fixture and skipif-no-ifcopenshell +Usability @code release:v4
(C) 2026-06-21 Author `notebooks/getting_started.ipynb`: load `scenarios/station_baseline.yaml`, run the sim, plot exit-time CDF and a heatmap from telemetry; reference it from README "How can I use it?" section +Usability @docs release:v3.1
(C) 2026-06-21 Add a `--debug` flag to `chiyoda.cli run` that dumps per-step telemetry to `out/<run>/debug_steps.jsonl` alongside existing CSV exports; keep off by default for perf +Usability @code release:v3.1
(D) 2026-06-21 Prepare `pyproject.toml` for PyPI publication: package metadata, console_scripts entry for `chiyoda = chiyoda.cli:main`, `python -m build` Makefile target, dry-run via `twine check`; do not publish without explicit approval +DevEx @config release:v4
(D) 2026-06-21 Add an opt-in stress-test profile harness `scripts/perf_regression_suite.py` running `transit_cbrn`, `transit_cbrn_10k`, and `wildfire_wui` with timing + memory captures; emit a CSV under `out/perf/` that CI can diff for regressions +Perf @code release:v4
